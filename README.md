# WebMCP Safety Check

Live: https://webmcp-safety-check.sociobot.in — built by the Param Factory (`browser-extension`).

See `.factory/brief.json` for the researched problem this solves and `.factory/design.md` for the visual system.

## Develop

```
npm install
npm run dev
npm test
npm run build   # -> dist/
```
